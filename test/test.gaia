arrocha teste{
	
	int value;
	double PI;
	char b;
	string language;
	bool repeat;
	
	pointer char* pointerChar;
	
	value = 0; 
	PI = 3.14;
	b = 'A';
	language = "Recife";
	repeat = true;
	
	oial("Apresentando a linguagem: "+language);
	
	arrudeia(repeat){
		
		se (value == 5){
			repeat = false;
		}senao{
			oial("Valor = "+value);
		}
		
		value = value + 1;
	}
	
	oial("value of b before: "+b);
	pointer pointerChar = b;
	pointer pointerChar = 'c';
	
	oial("value of b after: "+b);
	
	teste(b);
	teste2();
	
}

void teste (char i){ 
	oial("O parametro passado para o procediemnto é: "+i);
}

void teste2 (){ 
	double numerator;
	oia("Digite um valor para ser subtraido, multiplicado e dividido por 5: ");
	dizai(numerator);
	
	double subtraction;
	double multi;
	double division;
	
	subtraction = numerator - 5.0;
	multi = numerator * 5.0;
	division = numerator/5.0;
	
	oial(numerator +" - 5 = "+subtraction);
	oial(numerator +" * 5 = "+multi); 
	oial(numerator +"/5 = "+division); 
	 
	
}
