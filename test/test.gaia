arrocha teste{
	
	int b;
	pointer int* a;
	
	oia("value of b before: "+b);
	
	pointer a = b;
	pointer a = 1;
	
	oia("value of b after: "+b);
	
	pointer a = 10;
	
	oia("value of b after: "+b);
	
	teste(b);
	teste2();
}

void teste (int i){ 
	oia(i);
}

void teste2 (){ 
	oia("Sem Argumentos");
}
